query Country($country_code:ID!){
    country(code: $country_code) {
        code
        name
        capital
        emoji
        currency
        currencies
        languages {
            name
        }
        continent {
            name
        }
    }
}